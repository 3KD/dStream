{
    email admin@dstream.stream
}

dstream.stream, www.dstream.stream {
    # Next.js Web App
    reverse_proxy web:3000

    # HLS Streaming (MediaMTX)
    handle_path /stream/* {
        reverse_proxy mediamtx:8888
    }
}
