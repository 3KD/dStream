# Local Development Environment
# Copy to .env.local to use (never commit .env.local)

# HTTPS for LAN access (requires mkcert setup)
# Set to "true" to enable HTTPS dev server
HTTPS_ENABLED=false

# Stream Server (MediaMTX)
NEXT_PUBLIC_WHIP_URL=http://localhost:8881
NEXT_PUBLIC_HLS_URL=http://localhost:8882

# Nostr Relays (comma-separated)
NEXT_PUBLIC_NOSTR_RELAYS=wss://relay.damus.io,wss://relay.primal.net,wss://nos.lol,ws://localhost:8081

# Monero Node (optional - for payment verification)
# MONERO_NODE_URL=http://localhost:18081
